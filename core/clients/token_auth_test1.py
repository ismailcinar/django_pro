import imp
from urllib import response
import requests
from pprint import pprint

def client():
    credential = {
        'username' : 'testuser',
        'password' : 'testing321..'
    }

    response = requests.post(
        url = 
    )